// Stub auth types (no auth in this version)
export interface ClientFacingUserObject {
  email: string;
  firstName: string;
  lastName: string;
  profilePictureUrl: string;
  apiKey: string;
  createdAt: string;
  updatedAt: string;
}
